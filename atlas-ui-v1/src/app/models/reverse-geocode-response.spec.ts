import { ReverseGeocodeResponse } from './reverse-geocode-response';

describe('ReverseGeocodeResponse', () => {
  it('should create an instance', () => {
    expect(new ReverseGeocodeResponse()).toBeTruthy();
  });
});
